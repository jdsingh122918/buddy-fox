# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Already installed from main project
# claude-agent-sdk>=0.1.0
# anthropic>=0.40.0
# anyio>=4.0.0
# python-dotenv>=1.0.0

# Additional dependencies
# (Main project dependencies will be reused since backend imports from src/)
# Install all dependencies from root: pip install -r ../requirements.txt

# Transcription services (already included in root requirements.txt)
# assemblyai>=0.17.0
# websockets>=12.0
